<%- include("partials/header"); -%>

<h1>Home</h1>
<p><%= homeStartingContent %></p>
<p>
  <% posts.forEach(function(post){ %>
    <h2><%= post.titleBody %></h2>
    <p>
      <%= post.postBody.substring(0,100) + "..." %> 
      <a href=<%= "/posts/" + post.titleBody %>>Read More</a>
    </p>

   <% }); %>
</p>

<%- include("partials/footer"); -%>
